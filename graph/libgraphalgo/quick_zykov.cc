#include "quick_zykov.h"

namespace cavcom {
  namespace graph {

    QuickZykov::QuickZykov(const Graph &graph) : GraphAlgorithm(graph), k_(0) {}

    bool run() {
      return true;
    }

  }  // namespace graph
}  // namespace cavcom
